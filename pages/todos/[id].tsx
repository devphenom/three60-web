import SingleTodoWrapper from '@todos/components/single-todo/single-todo-wrapper/single-todo-wrapper';

type Props = {};

const Todo = (props: Props) => {
  return <SingleTodoWrapper />;
};

export default Todo;
